---
title: "Obter filtro"
description: "Obtém um filtro salvo específico por ID, restrito ao usuário autenticado."
source: api
contentType: referencia
method: GET
endpoint: /crm/saved-filters/{id}
---

Obtém um filtro salvo específico por ID, restrito ao usuário autenticado.

## Parâmetros de rota

<Params>
<Param name="id" type="int">
ID do filtro salvo.
</Param>
</Params>

## Exemplo

<CodeTabs labels={["cURL", "Node", "Python", "URL"]}>

```bash
curl https://apiv3.ihelpchat.com/api/v2/crm/saved-filters/45 \
  -H "Authorization: Bearer $IHELP_TOKEN"
```

```js
const res = await fetch('https://apiv3.ihelpchat.com/api/v2/crm/saved-filters/45', {
  headers: { Authorization: `Bearer ${process.env.IHELP_TOKEN}` },
});

console.log(res.status);
```

```python
import os, requests

r = requests.get(
    "https://apiv3.ihelpchat.com/api/v2/crm/saved-filters/45",
    headers={"Authorization": f"Bearer {os.environ['IHELP_TOKEN']}"},
    timeout=15,
)
print(r.status_code)
```

```http
GET https://apiv3.ihelpchat.com/api/v2/crm/saved-filters/45
```

</CodeTabs>

## Erros

| HTTP | `error` | Quando |
|---|---|---|
| 401 | - | Token ausente, inválido ou expirado |
| 404 | - | Filtro não encontrado ou pertence a outro usuário |
